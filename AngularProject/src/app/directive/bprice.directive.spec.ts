import { BpriceDirective } from './bprice.directive';

describe('BpriceDirective', () => {
  it('should create an instance', () => {
    const directive = new BpriceDirective();
    expect(directive).toBeTruthy();
  });
});
